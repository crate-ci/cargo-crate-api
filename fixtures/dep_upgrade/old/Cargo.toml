
[package]
name = "dep_upgrade_old"
version = "1.0.0"
edition = "2021"

[package.metadata.release]
release = false

[dependencies]
dep = { path = "../dep_old", version = "1.0", package = "dep_upgrade_dep" }
